# Gemini CLI 自訂指令:/note — 生底稿(Stage 1)
# 把 .md 渲染成 A4 白底底稿 base_pNN.png。前提:Playwright。
# 完整規格見 prompts/image_notes_skill.md。

description = "把一份 .md(預設 web/enhanced.md)渲染成 A4 白底底稿 base_pNN.png(原文保真、零遺漏)。底稿與身份無關,可重複給多職業視角用。下一步用 /好學生筆記 在底稿上生圖"

prompt = """
你要執行「好學生筆記 · 生底稿(Stage 1)」。完整規格見 prompts/image_notes_skill.md。

使用者參數(若空,預設來源 .md = web/enhanced.md):{{args}}

步驟:
1. 解析「來源 .md 路徑」。
2. python3 scripts/image_notes_session.py note <file.md>
   → 產 sessions/<slug>/note/base_pNN.png + meta.json(slug = 日期_檔名)。
3. 告訴使用者底稿位置與 slug,並提示下一步:/好學生筆記 <slug> <身份>。
"""
